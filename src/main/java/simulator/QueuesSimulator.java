package simulator;

import view.GUI;

// Clasa QueuesSimulator cu o metoda main in care se vor instantia obiecetele necesare simularii (GUI)
public class QueuesSimulator {
    public static void main(String[] args) {
        GUI view = new GUI();
        view.setVisible(true);
    }
}
